#include <stdio.h>
#include <ctype.h>
#include "buffer.h"

int main(int argc, char **argv) {
    // argv[1] = arquivo de entrada
    // argv[2] = arquivo de saida
    // argv[3] = numero de colunas
	return 0;
}